internal static class Extensions {
    //public static IEnumerable<Voxel> WithinPart1Bounds(this IEnumerable<Voxel> source)
    //    => source.Where(item =>
    //            item.x >= -50 && item.x <= 50 && 
    //            item.y >= -50 && item.y <= 50 &&
    //            item.z >= -50 && item.z <= 50);    
}